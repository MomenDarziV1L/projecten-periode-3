function loginInfoAlert( ){
    // Select the input id and get its value 
    var logUsername = document.getElementById("username").value;
    var logPassword = document.getElementById("password").value;

    // Display the values
    alert(logUsername + ", " + logPassword);
}
